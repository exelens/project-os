# Чеклист: агент в уже существующем репо

Перед крупной задачей:

1. Прочитай `AGENTS.md` + `docs/canon-owners.md` (если есть).
2. Трогаешь copy / CTA / empty на витрине или first screen → сверь `docs/voice.md` (если есть).
3. Найди owner понятия, которое трогаешь — не invent const.
4. Найди существующие тесты/parity рядом — расширь их, не плоди twin класса A.
5. Зона риска? — явный инвариант или второй проход (`prism/layers.md`).
6. Ошибки наружу — только sanitize/allowlist.
7. После кода: тесты на затронутый контур + обновление короткого doc, если менялся канон.
8. Не commit / не wipe `reviews/` без просьбы владельца.

Если просят «аудит всего» — предложи Prism, не один гигантский чат.

Между циклами Prism / перед короткой FIX-волной — стой [prism/pisdetscheck.md](../../prism/pisdetscheck.md) (scratch в корне; не слой, не merge в `B-*`). Не путать с generic «senior audit + улучши промпт».
