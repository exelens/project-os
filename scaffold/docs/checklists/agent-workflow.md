# Чеклист: агент в уже существующем репо

Перед крупной задачей:

1. Прочитай `AGENTS.md` + `docs/canon-owners.md` (если есть).
2. Найди owner понятия, которое трогаешь — не invent const.
3. Найди существующие тесты/parity рядом — расширь их, не плоди twin класса A.
4. Зона риска? — явный инвариант или второй проход (`prism/layers.md`).
5. Ошибки наружу — только sanitize/allowlist.
6. После кода: тесты на затронутый контур + обновление короткого doc, если менялся канон.
7. Не commit / не wipe `reviews/` без просьбы владельца.

Если просят «аудит всего» — предложи Prism, не один гигантский чат.
