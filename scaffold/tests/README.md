# Гигиена тестов

Дополни этот файл командами и маркерами своего стека.

## Запуск

```bash
# TODO: быстрый suite
# TODO: полный suite (e2e / integration отдельно)
```

## Достаточность

- Тесты проверяют **контракты поведения**, не частную реализацию.
- Запрещены `assert True`, пустые тесты, «тест ради зелёной галочки».
- Маркеры отделяют медленное/хрупкое: e2e · integration · load · network.
- Быстрый suite = default CI; медленное — явно.

## Изоляция

- Общие фикстуры клиента / БД — **одно** место, без локальных дублей.
- Shared test DB + rollback **или** ephemeral schema/DB на тест — выбери один канон и зафиксируй здесь.
- Сброс process-global state (rate limit, caches, queue) — autouse или явные маркеры.
- Не писать в prod DB; guard по имени БД / URL.

## Twins / слои похожести

| Класс | Смысл | Действие |
|-------|--------|----------|
| **A · True twin** | Один SUT, один слой, почти тот же assert | Один owner; twin удалить или 1-path smoke |
| **B · Layer** | Похожий исход, разные слои (unit ≠ HTTP ≠ UI) | **Не удалять**; зафиксировать роли |
| **C · False green** | Зелёный из-за кривого контура | Сначала контур, потом assert |

1. Owner + leftover в twin-файле.  
2. Один кластер cleanup = один шаг; красный гейт → стоп.  
3. Дыру после сноса twin закрывать **thin-тестом**.

## FE ↔ BE parity

Если один смысл на двух сторонах: SSOT → mirror → **parity-тест** как gate.

## Текст vs селектор (UI)

- Устойчивые якоря (`role`, `data-*`) — «блок есть».
- Точный текст — только где формулировка = требование.
- На сценарий: 1–2 смысловые фразы + селектор/контракт.

## Policy gates (рекомендуется)

CI валит job при: сырой `str(e)` клиенту · запрещённые datetime/event-loop паттерны · session anti-pattern.

## Мутационные тесты

Если стек поддерживает (e.g. `mutmut` для Python, `schemathesis` для API, `mutant` для Ruby):

```bash
# TODO: команда мутационных тестов
```

Мутации = проверка, что тесты реально ловят баги, а не просто зелёные.
