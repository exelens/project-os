# Project OS — пример env (секретов здесь нет)
# Замени значения на свои; реальный .env не коммитить.
APP_NAME=TODO_PROJECT_NAME
APP_ENV=development
APP_DEBUG=false

# БД
DB_HOST=localhost
DB_PORT=5432
DB_NAME=TODO_DB_NAME
DB_USER=TODO_DB_USER
DB_PASSWORD=TODO_DB_PASSWORD

# Токены — в переменные окружения хостинга, не в git
API_KEY=TODO_API_KEY
