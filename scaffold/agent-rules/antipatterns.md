# Антипаттерны (запреты)

## Канон и продукт

- ❌ Второй SSOT «временно в UI/роуте»
- ❌ Копипаста лимитов/лейблов без parity-гейта
- ❌ Маркетинговый claim без evidence / без honesty-дока

## Код и API

- ❌ `str(exception)` / traceback клиенту
- ❌ God-route: бизнес-логика + SQL + форматирование ошибок в одном хендлере без нужды
- ❌ Тихий `except Exception: pass` на критичном пути
- ❌ Блокировать request loop тяжёлой работой без offload

## Тесты

- ❌ `assert True` / пустой тест
- ❌ Дубль общих фикстур клиента/БД вне одного места
- ❌ Shared mutable process state без сброса между тестами
- ❌ Удалить «похожий» тест без класса twin A/B/C
- ❌ False green: assert на неправильной БД/моке/контуре

## Процесс / Prism

- ❌ Писать код на слое audit / merge / verify
- ❌ Мешать `B-*` и domain/optional ID в одном backlog
- ❌ Объявить цикл green при незапущенном слое как будто он пройден
- ❌ Агент удаляет `reviews/` после close
- ❌ ACCEPTED без причины и критерия пересмотра

## Git / агент

- ❌ Commit / push / force без явной просьбы
- ❌ Править git config
- ❌ Коммитить секреты «чтобы заработало»
